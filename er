We are preparing to implement the backend APIs for the contract intelligence service (to be deployed on OpenShift), and we want to ensure the 4 requested endpoints match your expectations:

RAG Search API

Metadata API

Report Query API

Report Lookup API

Our backend already supports vector-based search over contracts, Excel-based analysis, and document metadata, but we need your guidance on the exact behavior and payloads for each endpoint.

1) RAG Search API

We assume this should provide free-form Q/A over contract documents using our FAISS vector indexes (per client / country).

Could you please confirm:

Should this API accept any natural-language question and search within a specified client (and optionally country/segment)?

Should the response include:

an answer (LLM-generated), and

top N source snippets (document name + page numbers + file path)?

Is multi-client search needed, or should each call target exactly one client (and possibly one country)?

Any constraints on max number of sources or response length?

2) Metadata API

We maintain metadata per document (client, country, policy_id, file path, page range, modified time, plus additional manifest attributes).

Could you clarify:

What metadata should this API expose (e.g., client/country hierarchy, document list, contract IDs, status flags)?

Should it support filtering (client, country, document type, date, etc.)?

Expected request JSON and response JSON (which fields should be included)?

3) Report Query API

We can already generate structured answers and summaries using RAG over both contract PDFs and Excel-based data.

Please confirm:

Should this endpoint produce a structured JSON report for a selected client/country (e.g., COLA rules, renewal terms, key clauses)?

Is the intent to run predefined report templates, or support ad-hoc report-style questions?

Should the endpoint be synchronous (return full report in one call) or asynchronous (job submission → fetch result)?

4) Report Lookup API

We would like clarity on how this differs from the Metadata and Report Query APIs.

Is this endpoint meant to retrieve previously generated reports, or return specific data fields (e.g., COLA index, COLA frequency, renewal cycle)?

Should reports be retrieved by client/country/report-ID, or recalculated on demand?

Expected request/response format?

General Requirements (for all 4 APIs)

For each endpoint, could you please specify:

Exact purpose and functional expectations

Expected request JSON (required fields, filters, parameters)

Expected response JSON (structure & mandatory fields)

Authentication model (API key, OAuth, service account, etc.)

Whether the endpoint should operate synchronously or asynchronously

Follow-Up Meeting

For the above clarifications, we have a follow-up call tomorrow at 4 PM IST.
Hope to see you all there. Thanks.
