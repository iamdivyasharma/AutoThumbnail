diff --git a/orchestrator.py b/orchestrator.py
@@
-class RouteIn(BaseModel):
-    query: str
-    extra: Optional[Dict[str, Any]] = None
-    target: Optional[str] = None
-    targets: Optional[List[str]] = None
-    session_id: str = "default"
+class RouteIn(BaseModel):
+    query: str
+    extra: Optional[Dict[str, Any]] = None
+    target: Optional[str] = None
+    targets: Optional[List[str]] = None
+    session_id: str = "default"
+    answer_schema: Optional[Dict[str, Any]] = None  # NEW: allow framed JSON from route flow
+    debug: bool = False                              # NEW: hide per-bot answers unless debugging
@@
-    if len(results) > 1:
-        answer = compose_final(q, results, answer_schema=None)
-        answers_by_target = sanitize_map(results)
-    else:
-        answer = sanitize_answer(next(iter(results.values()), ""))
-        answers_by_target = None
+    if len(results) > 1:
+        # NEW: score & pick best per target before composing (reduces noise)
+        observations = [
+            {"step_index": i, "step": {"target": t, "input": q}, "result": r}
+            for i, (t, r) in enumerate(results.items())
+        ]
+        latest = score_observations(q, observations) or results
+        answer = compose_final(q, latest, answer_schema=inp.answer_schema)
+        answers_by_target = sanitize_map(results) if inp.debug else None
+    else:
+        answer = sanitize_answer(next(iter(results.values()), ""))
+        answers_by_target = sanitize_map(results) if inp.debug else None
@@
-        answers_by_target=answers_by_target or None,
-        errors=(errors or None)
+        answers_by_target=answers_by_target or None,
+        errors=(errors or None)
     )




@@
-COMPOSER_SYSTEM = (
-    "You are a response composer. Combine multiple tool answers (air/hotel/tdna) into ONE coherent, concise reply. "
-    "Synthesize and prioritize; resolve conflicts; respect filters and prior choices. "
-    "If answer_schema is provided, output STRICT JSON following it (no prose). Otherwise produce clean prose. "
-    "No greetings or tool prefixes."
-)
+COMPOSER_SYSTEM = (
+    "You are a response composer. Combine multiple tool answers (air/hotel/tdna) into ONE unified reply. "
+    "Prioritize relevance to the user_query, resolve conflicts, and prefer the strongest evidence. "
+    "Never echo raw per-tool sections or labels; produce a single, cohesive answer. "
+    "If answer_schema is provided, output STRICT JSON that validates against it (no prose). "
+    "If no schema, produce ≤8 sentences of clean prose focused on the actual ask."
+)
